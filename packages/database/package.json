{
  "name": "@classia/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"lint database pendiente\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"test database pendiente\"",
    "build": "echo \"build database pendiente\"",
    "prisma": "dotenv -e ../../.env.example -e ../../.env -- prisma",
    "db:generate": "dotenv -e ../../.env.example -e ../../.env -- prisma generate",
    "db:migrate": "dotenv -e ../../.env.example -e ../../.env -- prisma migrate dev",
    "db:migrate:init": "dotenv -e ../../.env.example -e ../../.env -- prisma migrate dev --name init_backend_v1",
    "db:studio": "dotenv -e ../../.env.example -e ../../.env -- prisma studio",
    "seed:demo": "dotenv -e ../../.env.example -e ../../.env -- tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "dotenv-cli": "^7.4.4",
    "prisma": "^5.22.0",
    "tsx": "^4.19.0"
  }
}
